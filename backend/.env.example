# Flask Configuration
FLASK_HOST=0.0.0.0
FLASK_PORT=5000
FLASK_ENV=development
FLASK_DEBUG=False

# Security - IMPORTANT: Generate a secure random key for production
# You can generate one with: python -c "import secrets; print(secrets.token_hex(32))"
SECRET_KEY=your-secret-key-here

# CORS Configuration - CRITICAL SECURITY SETTING
# Development: Defaults to localhost origins if not set
# Production: MUST be explicitly set - no wildcards allowed
# 
# Examples:
# Development:
# CORS_ORIGINS=http://localhost:3000,http://127.0.0.1:3000
# 
# Production (REQUIRED):
# CORS_ORIGINS=https://yourdomain.com,https://www.yourdomain.com
# 
# Multiple domains:
# CORS_ORIGINS=https://yourdomain.com,https://subdomain.yourdomain.com,https://cdn.yourdomain.com
CORS_ORIGINS=http://localhost:3000,http://127.0.0.1:3000